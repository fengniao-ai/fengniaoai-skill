# 电商套图视觉与 Prompt

用于把商品事实、套图用途和渠道要求编译成可直接传给 `/api/v1/img/generate` 的 Markdown `prompt`。公开版本只保留通用执行规则；不要要求生图模型返回 JSON，也不要把本文件原样放进 Prompt。

## 核心原则

发生冲突时按以下顺序取舍：

```text
商品真实与用户明确要求
> 平台硬性规则
> 当前图片用途
> 用户品牌与风格偏好
> 品类与目标市场的一般经验
```

先把输入分为三类：

- **可见事实**：商品类型、主色、外形、材质、包装、可辨认文字和已有配件。
- **用户确认**：核心卖点、品牌要求、目标人群、精确文案和禁止事项。
- **不可推断**：功效、性能数值、认证、价格、折扣、赠品、未展示配件和品牌承诺。

只使用前两类信息，不把第三类写入方案或 Prompt。

- 看不清的颜色、材质和文字不要猜测；保持商品固有颜色，不虚构 Hex 色值。
- 平台和市场可以影响构图、信息密度、光线、氛围和缺省产物语言，但不能覆盖商品事实或用户明确要求。

## 商品一致性锁

套图策划时先根据可见事实和用户确认信息建立商品身份锁。未知项写“未知，不补全”，不要猜测；同一 SKU 或颜色组逐字复用同一份身份锁，整套图片复用相同视觉基线。多色产品先建立共通结构事实，再为每个颜色/SKU 分别记录颜色、图案、Logo 和包装差异，不能用一个颜色的身份锁覆盖其他颜色。单袋、整卷、使用状态或组合数量属于“本张展示约束”，不能写进身份锁。

```markdown
## 商品身份锁

- 主色与配色：{可见事实}
- 外形与比例：{可见事实}
- 材质与表面：{可见事实}
- 按钮、接口与结构：{可见事实}
- Logo、包装与文字位置：{可见事实}
- 已确认配件：{列表或无}
- 禁止变化：商品身份、颜色、轮廓、结构、Logo、包装、文字位置和配件种类
```

### 多图参考选择

先为原图保留稳定文件 ID，再按以下顺序为每张任务选择参考图：

1. **按变体固定参考图 1**：单色或单 SKU 套图选择商品完整、清晰、无遮挡、少道具、少营销文字的原图；同一颜色/SKU 的任务始终使用同一张身份主图。多色产品按颜色/SKU 分组，各组使用自己的身份主图，不能让颜色 A 的主图约束颜色 B。不能把人物演示、多商品场景、尺寸说明或营销信息图作为身份主图。
2. **只补本张需要的证据图**：按任务再加入零至两张真实原图，用于角度、结构、材质、包装、尺寸或使用状态。默认总计不超过三张，不能因为素材存在就把全部图片传给每个任务。
3. **顺序和名称必须对应**：`reference_roles` 写成“`main-005｜商品身份主图`”这类“稳定 ID + 职责”；Prompt 只能引用“参考图 1（main-005）”，不能只写 Agent 自己知道、模型无法映射的文件名。
4. **风格参考最后放**：只有用户认可的主视觉才可作为最后一张风格参考，并明确“只参考光影与氛围，不借用商品、Logo、包装、文字或配件”。原商品图始终排在它前面。

如果全套任务确实使用完全相同的两至三张参考图，放入批次 `shared` 以复用上传；某张任务需要额外证据时，在该 task 中传入包含身份主图在内的完整有序参考图与等长职责，覆盖 `shared`，不能只传新增的一张。

### 多色与多 SKU

- **分别出图**：规划表增加“目标颜色/SKU”列；每张只传目标变体的身份主图和相关证据图，使用默认 `reference_mode=single_subject`。同一变体内部固定参考图 1，不同变体可以不同。
- **多变体同框比较**：每个颜色/SKU 只选一张干净身份图，按用户确认的展示顺序传入，设置 `reference_mode=variants`；`reference_roles` 分别写明稳定 ID、颜色/SKU 和职责。Prompt 明确各变体的位置或顺序，不允许颜色、图案、Logo、包装和规格互相混合。
- **超过六个变体**：拆成多张比较图，或让用户提供可信的组合参考图；不能省略部分变体后声称已经展示全部。
- 风格参考不属于商品变体，仍只能放在所有商品原图之后，并受“只参考风格”的限制。

### 参考素材支持角度

规划前按真实素材记录可用视图，例如“正面：main-001；佩戴：main-004；侧面：无”。没有对应参考证据时，不规划侧面、背面、内部、开口、接口、耳堵、包装背面或未展示配件；改用已有证据支持的正面、局部细节、组合陈列或佩戴效果。用户明确要求生成无参考角度时，先说明模型会推断结构并获得确认，不能把推断结果标记为商品事实。

## 意图与默认资产范围

平台名称决定渠道规则，用户表达的资产类型决定实际工作范围。明确的“只做、不要、不需要”优先于任何默认值。

| 用户表达 | 默认规划 | 不自动包含 |
| --- | --- | --- |
| Amazon 主图 | 1 张符合当前站点要求的主图 | 副图、A+ |
| Amazon Listing、主副图 | 主图与用途不同的副图 | A+ |
| Amazon A+ | 按具体 A+ 模块规划内容图 | Listing 主副图 |
| Amazon 套图 | 默认先规划主副图，并在同一次确认中询问是否增加 A+ | 未确认的 A+ |
| Amazon 全套、主副图加 A+ | 分区规划 Listing 与 A+ | 无 |
| 淘宝/天猫主图、套图 | 默认 5 张用途不同、设计丰富的商品主图 | 白底图、详情页 |
| 淘宝/天猫详情页 | 默认 6–10 个独立详情模块，信息适中时以 8 个为基准 | 5 张主图、整页拼接长图（当前不支持） |

淘宝/天猫 5 张主图优先规划首图、核心卖点、使用场景、材质/功能细节，以及规格/SKU/包装/第二场景中的一个。根据商品证据调整，不重复构图凑数；只有用户明确要求白底图时才加入。

Amazon A+ 不是 Listing 副图。先确认 Standard/Premium 和站点；用户未指定具体模块时，按商品信息建议一组精简模块并等待确认，不能把 A+ 当成固定数量、统一比例的普通套图。没有品牌故事、参数、认证或系列对比证据时删除相关模块，不补造内容。

## 套图规划

按用户数量选择不同用途，不固定七张，也不重复构图凑数量：

- **渠道主图**：按目标平台规划；Amazon 主图通常使用纯白背景，淘宝/天猫默认使用突出商品和核心购买理由的丰富设计，不默认白底。
- **核心卖点图**：一张只表达一个用户确认的购买理由。
- **使用场景图**：真实环境、合理尺度，商品仍是视觉中心。
- **功能说明图**：操作关系明确，不虚构功能或内部结构。
- **细节材质图**：合理近景、真实纹理、保持商品颜色。
- **尺寸规格图**：留出标注空间，数值必须由用户提供。
- **包装信任图**：只展示参考图或用户确认存在的包装与配件。

数量较少时优先主图、核心卖点和使用场景，再按需要加入功能、细节、规格与信任图。

执行前的规划表至少包含以下列，先完成参考图分配再写 Prompt：

| 任务 ID | 资产类型/模块 | 目标颜色/SKU | 用途 | 本张展示约束 | 有序参考图 | 参考理由 | 生成比例 | 最终交付尺寸 | 交付处理 | 语言 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| amazon-main-01 | Listing 主图 | 米白红字款 | 白底主图 | 单件完整展示 | 参考图 1（main-005） | 本变体干净完整的身份基准 | 1:1 | 按当前站点要求 | 生成后校验 | 无新增文字 |

同一文件在表格、`reference_images`、`reference_roles` 和 Prompt 中的编号必须一致。引用的卖点或结构必须能在本张选用的证据图或用户确认信息中找到；只存在于未传参考图中的信息不能写成模型可见证据。

## 渠道与市场

只向 Prompt 写入与本张图有关的要求。平台未指定时使用“通用电商”，不默认某个国家或平台风格。

- **Amazon**：主图遵循纯白背景、完整商品、无道具和促销文字等常见要求；副图强调真实场景与可信信息。
- **内容电商**（TikTok Shop、抖音）：移动端和首屏吸引力优先，画面直接，卖点短而清晰。
- **综合电商**（淘宝、天猫、京东、拼多多）：商品突出、信息层级清楚；可有营销氛围，但不能编造促销信息。
- **OZON**：商品图只使用 `3:4`，不规划其他比例；商品中心化、移动端易读，套图的信息设计应丰富且用途有明显差异，可按实际证据组合核心卖点、场景、功能/材质细节、尺寸/SKU、包装或使用说明。具体数量、内容密度和表现形式始终由用户诉求、商品实际情况与已确认卖点决定，不固定套版、不重复构图，也不为追求丰富而虚构信息。
- **其他跨境平台**（Temu、Shopee、Lazada、AliExpress）：移动端易读、商品中心化、减少文化依赖；有可见文案时使用按下方规则解析的产物语言。
- **B2B 平台**（Alibaba）：突出规格、应用与可信度，只使用用户确认的工艺和资质。
- **品牌站**（Shopify）：优先品牌一致性、编辑式摄影和生活方式表达。

目标市场只作为视觉软偏好：结合当地文化习惯、信息密度和生活场景自然调整，避免刻板印象。用户要求平台合规时，提醒其上架前按平台当前政策复核。

### 产物语言默认规则

仅在成图包含标题、卖点或行动文案时解析产物语言；没有可见文字时不询问。按以下优先级使用第一个可确定的值：

1. 用户明确指定的产物语言。
2. 用户明确指定的站点或目标市场语言。
3. 平台默认语言。
4. 仍无法可靠判断时询问用户。

Agent 的对话语言不参与选择。用户提供的精确文案逐字保留；若精确文案与解析后的语言冲突，先询问是否需要翻译，不得静默翻译或改写。

| 平台 | 未指定语言时的默认值 |
| --- | --- |
| 淘宝、天猫、京东、拼多多、抖音 | 简体中文 |
| OZON | 俄语 |
| Amazon | 英语；已明确站点或目标国家时，使用对应市场语言 |
| Alibaba.com | 英语 |
| Temu、Shopee、Lazada、AliExpress、TikTok Shop | 优先目标市场语言；目标市场不明时询问 |
| Shopify | 优先已有品牌或店铺语言；无法判断时询问 |
| 通用电商 | 有可见文案时询问 |

### 渠道与用途推荐比例

以下是 Skill 的生成默认值，不是平台永久有效的强制政策。接口支持 `1:1`、`3:4`、`4:3`、`9:16`、`16:9`、`2:3`、`3:2` 和带参考图的 `original`。

| 渠道与用途 | 默认比例 | 可选比例 |
| --- | --- | --- |
| Amazon 主图与副图 | `1:1` | 场景副图可用 `4:3` |
| OZON 商品图 | `3:4` | 不使用 `1:1` |
| Temu、Shopee、Lazada、AliExpress 商品图 | `1:1` | 移动端纵图可用 `3:4` |
| 淘宝、天猫、京东、拼多多商品主图 | `1:1` | 纵向卖点图可用 `3:4` |
| TikTok Shop、抖音商品卡 | `1:1` | 内容流和竖版封面使用 `9:16` |
| Alibaba B2B 商品图 | `1:1` | 应用场景可用 `4:3` 或 `3:2` |
| Shopify 商品列表 | `1:1` 或 `3:4` | 横幅可用 `16:9` 或 `3:2` |
| 通用电商 | `1:1` | 纵图 `3:4`，横图 `4:3` |

- 平台未明确排除某个比例时，用户明确选择的接口支持比例优先；平台明确排除的比例不能被通用偏好覆盖。
- OZON 商品图必须在每个生成任务中显式传递 `aspect_ratio=3:4`，不能依赖 `image transform` 的 `original` 默认值。用户坚持 `1:1` 时，说明该比例不能作为 OZON 标准商品图交付，并询问是否改为通用电商图片；未确认改类前不执行付费生成，也不能把结果标记为 OZON 交付图。
- 用户要求接口不支持的比例时，不静默替换；说明最接近的支持比例，或告知需要额外裁切工具。
- 同一图片需要多个比例时，每个比例是独立计费操作，必须在执行前列入总操作数。
- 平台政策可能变化；用户要求严格合规时，提示按当前平台规则复核比例、像素和安全区。

### Amazon A+ 模块

A+ 使用模块尺寸，不使用一个全局比例。下表是常见 Standard A+ 设计参考，不是所有站点和模块永久不变的硬规则；提交前按当前 Seller Central 模块编辑器复核。

| 常见模块资产 | 常见交付尺寸 | 生成方式 |
| --- | --- | --- |
| Header 主视觉 | `970×600` | 用接近的支持比例生成，保留裁切安全区后确定性裁切 |
| 横幅或文字叠加背景 | `970×300` | 使用横图生成，核心内容避开上下裁切区 |
| 三图模块单图 | `300×300` | `1:1` 生成后缩放 |
| 四图模块单图 | `220×220` | `1:1` 生成后缩放 |

- A+ 规划表必须写具体模块、生成比例、最终尺寸和裁切/缩放方式。
- 用户只说“Amazon 套图”时不自动加入 A+；在主副图规划确认中询问一次即可。
- 没有确定性裁切工具时，只能交付 A+ 内容图并标记“待按模块尺寸排版”，不能宣称可直接上传。

### 淘宝/天猫详情页模块

详情页默认按移动端优先的独立模块设计，不把整页作为一次生图请求；当前 Skill 只交付独立模块图，不提供长图拼接。

- 默认根据商品信息规划 6–10 个模块，信息适中时以 8 个为基准；素材不足时减少模块，不虚构卖点凑数量。
- 每个模块使用接口支持的比例独立提交、独立计费、独立验收。移动详情默认优先 `3:4`，内容较长可用 `2:3`，方形信息块可用 `1:1`，确需横向模块时再用 `4:3` 或 `3:2`；不要把整页所需的极端长比例传给生图接口。
- 移动端内容图可按约 `750px` 宽准备；这是常用交付建议，不是所有类目和装修工具永久不变的强制值。PC 版按店铺模板和发布后台当前要求适配。
- 规划顺序优先为商品定位、核心卖点、场景、功能/材质细节、规格/SKU、包装/使用/信任信息；没有证据的模块删除或替换。
- 不主动询问用户是否需要拼接，也不把拼接列为可选交付物。用户明确要求“长图、整页、一张上传”时，说明当前 Skill 暂不支持拼接，提供继续生成独立模块图的选择；不能通过一次生图或再次生图模拟整页长图。

## 品类视觉参考

品类只提供默认方向，用户风格和商品事实优先：

- 科技数码：均匀柔光、干净留白、中性色调、结构清晰。
- 游戏设备：冷色侧光、适度暗背景、力量感；不能牺牲商品细节。
- 家居用品：自然暖光、柔和通透、真实居住氛围。
- 运动装备：自然光、明快清晰、动作感和阳光活力。
- 服饰配件：柔和侧光、真实面料纹理、优雅克制。
- 美妆护肤：均匀柔光、干净透亮、精致材质；不虚构功效。
- 食品饮料：自然食欲感、真实色泽、干净卫生；不虚构配料和营养信息。
- 工具家电：功能关系明确、结构可信、使用环境合理。

默认真实商业摄影。只有用户明确要求插画、3D、动漫或其他形式时才改变媒介风格。

## 风格交互

用户没有明确风格偏好或说“你来定”时，Agent 自动选择方向，不增加选择步骤。

用户明确想看不同方向时，展示 3–4 套简短 Markdown 方案。方案应有明显差异；全局风格不写具体场景和道具。

```markdown
### 方案：{通俗风格名}

- 适合：{适用图片用途}
- 光影：{可跨图片复用的光线}
- 色彩：{商品原色与背景、强调色关系}
- 氛围：{可跨图片复用的商业氛围}
- 推荐原因：{不超过一句话}
```

## 生图 Prompt 模板

为每张图单独填写。将完成后的整段 Markdown 作为 `prompt` 字符串传给 `image transform`，参考商品图通过 `image`、`reference_images` 或 `reference_image_urls` 单独传递；多图同时传等长的 `reference_roles` 固定职责。

```markdown
# 电商图片任务

- 用途：{本张图片用途}
- 平台与市场：{目标平台和市场；未指定则写通用电商}
- 产物语言：{按产物语言优先级解析后的语言；没有可见文字则写“无文字要求”}
- 比例：{画面比例}

## 商品事实与卖点

- 商品：{参考图中确认的商品事实}
- 本张卖点：{一个用户确认的卖点；没有则不写}

## 必须保持

- 保持参考图商品的造型、比例、固有颜色、材质、包装结构、品牌标识和可见文字
- 不增加未经确认的配件、功能或商品结构；看不清的文字不要重造

{逐字放入本套商品身份锁}

## 本张展示约束

- 展示数量与状态：{单件、整卷、使用中或用户确认的组合；仅约束本张}
- 本张允许变化：{背景、动作、摆放或构图}

## 渠道要求

{仅写当前平台和市场对本张图片有用的要求}

## 套图统一视觉基线

- 光线：{全套共用光线}
- 色彩：{保持商品原色的统一色彩关系}
- 氛围：{全套共用商业氛围}

## 本张画面

- 参考证据：{按实际接口顺序列出“参考图 N（稳定 ID）”；没有证据的角度不得生成}
- 场景与构图：{场景、商品位置、占比和必要道具}
- 唯一表达重点：{本张图片的单一任务}
- 画面文案：{用户提供的精确文案逐字保留；否则只用目标语言表达用户确认的卖点；没有文字需求则删除}

## 禁止事项

- 不改变商品颜色，不遮挡关键结构，不生成重复商品或错误配件
- 不生成参考素材未展示的侧面、背面、内部、接口、扣件、耳堵或包装结构
- 不虚构功能、规格、认证、价格、促销、赠品和品牌信息
- 有精确画面文案时，不增加角度标签、说明文字、徽章、水印或其他可见文字
- 不使用浓重滤镜，不让场景和装饰抢过商品主体
```

删掉不适用的章节和所有占位符。白底主图只保留商品事实、保持要求、平台主图规则与纯白背景要求。

## 执行检查

- 每张使用新的 `request_id`，始终把原商品图作为参考。
- 每张参考图 1 使用目标颜色/SKU 的身份主图；同一变体内保持一致，只追加与本张任务直接相关的证据图，默认总计不超过三张。
- 提交前逐项核对参考图数组、职责和 Prompt 编号；三者不一致时不得开始付费生成。
- 同一套图逐字复用“统一视觉基线”，只改变本张画面。
- 已有用户认可的主视觉时，可追加为风格参考，不能替代原商品图。
- 白底主图默认使用 `image transform` 并明确纯白背景、商品居中和一致性要求。只有用户明确要求抠图、去背景、移除背景或抠出主体时，才使用抠图接口；“白底图”本身不是抠图意图。
- 精确文字、商品一致性和平台合规不作绝对承诺；本地化优先使用图片翻译。
- 执行前列出每张用途、比例、语言、点数和总操作数并获得确认。
- 交付时优先展示结果图、用途、`local_path` 和点数；request ID 与远程 URL 保留用于按需查看和问题排查，不默认堆给用户。

## 生成结果一致性检查

批量生成与视觉验收必须解耦。生成成功只以批次 manifest 的任务终态和产物落盘为准；视觉读取不能成为生成完成、滑动队列补位、批次恢复或结果交付的门禁。先交付批次汇总、结果目录和已完成文件，再进行视觉初检；大批量只展示汇总与最近结果，不在对话铺开数百条路径。

有能力查看结果图时逐张对照商品一致性锁。图片工具明确支持单次超时、取消和终态回收时，视觉验收最多 2 张并发，并为每张设置不超过 60 秒的工具超时；缺少其中任一能力时不自动调用视觉读取，直接展示结果并标记“等待商家确认”。用户在交付后明确要求 AI 检查时才一次读取 1 张，仍不得并行。不得一次并行打开整批图片，不得用临时 Shell 脚本编排生成或验收。读取失败、超时或工具不可用时继续交付；无法查看时不要声称已经通过视觉验收，也不能把已完成生图改判为失败。

- 商品数量、主色、轮廓和整体比例是否一致。
- 材质、按钮、接口、开口和关键结构是否变化或畸变。
- Logo、包装、可见文字的位置和内容是否明显错误。
- 是否新增、删除或替换了配件，是否生成重复商品。
- 商品与人物、家具和环境的尺度是否合理。
- 是否符合本张用途、选定比例和统一视觉基线。
- 是否出现未经确认的规格、功效、认证、价格、促销或赠品。

检查状态只使用“AI 初检通过”“需修正”“等待商家确认”。即使 AI 初检通过，也不能宣称已经符合平台最新政策或可以直接上架；商品细节、文字与合规性仍由商家最终确认。

发现明显漂移时，将该图片标记为“需修正”，说明具体问题并保留已经生成的结果。生成成功可能已经扣点，不自动再次付费生成；先告知用户重试成本并获得确认，再使用新的 `request_id` 修正。
